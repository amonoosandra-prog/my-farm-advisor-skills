# Grower Web Map

Generates lightweight interactive HTML field maps per grower/farm using
only Python stdlib. Each map is a self-contained `<5 KB` HTML file with
Leaflet.js, clickable field popups, and a sidebar field list.

See `AGENTS.md` for usage.
